import DataTable from '../../components/DataTable';import {PageHeader} from '../../components/Ui';import {orders} from '../../data/dummyData';
export default function DeliveryHistory(){const rows=orders.map((o,i)=>({...o,delivery:i===3?'Failed Delivery':'Delivered'}));return <><PageHeader title="Delivery history" description="A complete record of your previous stops."/><section className="panel"><DataTable rows={rows} columns={[{key:'id',label:'ORDER',render:r=><b>{r.id}</b>},{key:'customer',label:'CUSTOMER'},{key:'address',label:'ADDRESS'},{key:'date',label:'DATE'},{key:'total',label:'TOTAL',render:r=>`$${r.total.toFixed(2)}`},{key:'delivery',label:'RESULT',status:true}]}/></section></>}
