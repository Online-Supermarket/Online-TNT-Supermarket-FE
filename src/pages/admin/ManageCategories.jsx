import {Plus} from 'lucide-react';import {PageHeader} from '../../components/Ui';import useApiCollection from '../../hooks/useApiCollection';
export default function ManageCategories(){const {data:products}=useApiCollection('/api/products');const categories=[];return <><PageHeader title="Categories" description="Organize products into easy-to-shop collections." action={<button className="btn btn-primary"><Plus/> Add category</button>}/><div className="admin-category-grid">{categories.map(c=><article className="panel" key={c.id}><span style={{background:c.color}}>{c.icon}</span><div><h3>{c.name}</h3><p>{products.filter(p=>p.category===c.id).length} products</p></div><button className="btn btn-light btn-small">Edit</button></article>)}</div></>}
