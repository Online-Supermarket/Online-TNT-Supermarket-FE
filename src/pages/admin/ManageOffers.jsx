import {Plus,CalendarDays} from 'lucide-react';import {PageHeader,Status} from '../../components/Ui';import useApiCollection from '../../hooks/useApiCollection';
export default function ManageOffers(){const {data:products}=useApiCollection('/api/products');return <><PageHeader title="Offers" description="Create and manage promotional campaigns." action={<button className="btn btn-primary"><Plus/> Create offer</button>}/><div className="admin-offer-grid">{products.filter(p=>p.discount).map(p=><article className="panel" key={p.id}><img src={p.image}/><div><Status>Active</Status><h3>{p.name}</h3><p><CalendarDays/> Sep 01 – Sep 14, 2026</p><div className="offer-stats"><span><small>DISCOUNT</small><b>{p.discount}% off</b></span><span><small>SALE PRICE</small><b>Rs. {p.price.toFixed(2)}</b></span></div><button className="btn btn-outline full">Edit offer</button></div></article>)}</div></>}
