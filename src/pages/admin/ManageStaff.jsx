import {Plus} from 'lucide-react';import DataTable from '../../components/DataTable';import {PageHeader} from '../../components/Ui';import {staff} from '../../data/dummyData';
export default function ManageStaff(){return <><PageHeader title="Staff" description="Manage store operations team accounts." action={<button className="btn btn-primary"><Plus/> Add staff</button>}/><section className="panel"><DataTable rows={staff} columns={[{key:'name',label:'TEAM MEMBER',render:r=><div className="table-person"><span>{r.name[0]}</span><div><b>{r.name}</b><small>{r.email}</small></div></div>},{key:'phone',label:'PHONE'},{key:'role',label:'ROLE',render:()=> 'Store Staff'},{key:'status',label:'STATUS',status:true}]}/></section></>}
