import {MoreHorizontal} from 'lucide-react';import {Empty,Status} from './Ui';
export default function DataTable({columns,rows}){return <div className="table-wrap"><table><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}<th></th></tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={r.id||i}>{columns.map(c=><td key={c.key}>{c.render?c.render(r):c.status?<Status>{r[c.key]}</Status>:r[c.key]}</td>)}<td><button className="row-menu"><MoreHorizontal/></button></td></tr>):<tr><td colSpan={columns.length+1}><Empty title="No records yet" text="Connect the backend to load live data."/></td></tr>}</tbody></table></div>}
