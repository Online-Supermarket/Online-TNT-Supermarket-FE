import {Eye,Plus,Star} from 'lucide-react';import {useCart} from '../context/CartContext';
export default function ProductCard({product}){const {addToCart}=useCart();const imageSrc = product.imageUrl || product.image || 'https://images.unsplash.com/photo-1542838132-92c53300491e?auto=format&fit=crop&w=180&q=80'; const hasStock = product.stock > 0 || product.stockQuantity > 0; return <article className="product-card"><div className="product-image"><img src={imageSrc} alt={product.name}/>{product.discount&&<span className="discount">-{product.discount}%</span>}<button className="quick-view" aria-label="View details"><Eye size={18}/></button></div><div className="product-content"><span className="eyebrow">{product.category}</span><h3>{product.name}</h3><div className="rating"><Star size={14} fill="currentColor"/> {product.rating} <span>· {product.unit}</span></div><div className="product-bottom"><div><strong>Rs. {product.price.toFixed(2)}</strong>{product.oldPrice&&<del>Rs. {product.oldPrice.toFixed(2)}</del>}</div><button disabled={!hasStock} onClick={()=>addToCart(product)} aria-label={`Add ${product.name} to cart`}><Plus/>{hasStock?'Add':'Out'}</button></div></div></article>}
