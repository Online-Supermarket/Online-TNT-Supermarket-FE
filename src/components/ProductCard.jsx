import {Eye,Plus,Star} from 'lucide-react';import {useCart} from '../context/CartContext';
export default function ProductCard({product}){const {addToCart}=useCart();return <article className="product-card"><div className="product-image"><img src={product.image} alt={product.name}/>{product.discount&&<span className="discount">-{product.discount}%</span>}<button className="quick-view" aria-label="View details"><Eye size={18}/></button></div><div className="product-content"><span className="eyebrow">{product.category}</span><h3>{product.name}</h3><div className="rating"><Star size={14} fill="currentColor"/> {product.rating} <span>· {product.unit}</span></div><div className="product-bottom"><div><strong>${product.price.toFixed(2)}</strong>{product.oldPrice&&<del>${product.oldPrice.toFixed(2)}</del>}</div><button disabled={!product.stock} onClick={()=>addToCart(product)} aria-label={`Add ${product.name} to cart`}><Plus/>{product.stock?'Add':'Out'}</button></div></div></article>}
