import {Clock,ShoppingBag} from 'lucide-react';import {useCart} from '../context/CartContext';
export default function OfferCard({product}){const {addToCart}=useCart();return <article className="offer-card"><img src={product.image} alt={product.name}/><div className="offer-copy"><span className="discount-pill">SAVE {product.discount}%</span><h3>{product.name}</h3><p><Clock size={15}/> Offer ends September 14, 2026</p><div className="offer-price"><del>${product.oldPrice?.toFixed(2)}</del><strong>${product.price.toFixed(2)}</strong></div><button className="btn btn-primary" onClick={()=>addToCart(product)}><ShoppingBag size={18}/> Add to cart</button></div></article>}
