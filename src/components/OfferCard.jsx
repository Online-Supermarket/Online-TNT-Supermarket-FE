import {Clock,ShoppingBag} from 'lucide-react';import {useCart} from '../context/CartContext';
export default function OfferCard({product}){const {addToCart}=useCart();const imageSrc = product.imageUrl || product.image || 'https://images.unsplash.com/photo-1542838132-92c53300491e?auto=format&fit=crop&w=180&q=80'; return <article className="offer-card"><img src={imageSrc} alt={product.name}/><div className="offer-copy"><span className="discount-pill">SAVE {product.discount}%</span><h3>{product.name}</h3><p><Clock size={15}/> Offer ends September 14, 2026</p><div className="offer-price"><del>Rs. {product.oldPrice?.toFixed(2)}</del><strong>Rs. {product.price.toFixed(2)}</strong></div><button className="btn btn-primary" onClick={()=>addToCart(product)}><ShoppingBag size={18}/> Add to cart</button></div></article>}
