import {BadgePercent,BarChart3,Boxes,Circle,CircleUserRound,ClipboardList,History,LayoutDashboard,LogOut,MapPinned,Package,Shapes,Truck,UserCog,Users} from 'lucide-react';import {NavLink} from 'react-router-dom';import Logo from './Logo';import {useAuth} from '../context/AuthContext';
const iconMap={BadgePercent,BarChart3,Boxes,CircleUserRound,ClipboardList,History,LayoutDashboard,MapPinned,Package,Shapes,Truck,UserCog,Users};
export default function Sidebar({items,role}){const {logout,user}=useAuth();return <aside className="sidebar"><Logo light/><div className="workspace-label">{role} workspace</div><nav>{items.map(({to,label,icon})=>{const I=iconMap[icon]||Circle;return <NavLink key={to} to={to}><I/>{label}</NavLink>})}</nav><div className="sidebar-user"><div className="avatar">{user?.name?.split(' ').map(x=>x[0]).join('').slice(0,2)||role[0]}</div><div><strong>{user?.name||`${role} user`}</strong><span>{user?.email}</span></div><button onClick={logout} title="Logout"><LogOut/></button></div></aside>}
