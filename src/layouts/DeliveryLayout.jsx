import PortalLayout from './PortalLayout';const items=[['/delivery/dashboard','Dashboard','LayoutDashboard'],['/delivery/assigned','Assigned Deliveries','MapPinned'],['/delivery/history','Delivery History','History'],['/delivery/profile','Profile','CircleUserRound']].map(([to,label,icon])=>({to,label,icon}));export default function DeliveryLayout(){return <PortalLayout role="Delivery" items={items}/>}
